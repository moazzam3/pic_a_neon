const config = {
  baseURL: import.meta.env.VITE_BACKEND_SERVICES + '/api',
  imageBaseURL: import.meta.env.VITE_BACKEND_SERVICES + '/',
  cartKeyForLocalStorage : 'pic_a_neon_cart',
};

export default config;